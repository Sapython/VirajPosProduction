import { TableGroupsPipe } from './table-groups.pipe';

describe('TableGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new TableGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
