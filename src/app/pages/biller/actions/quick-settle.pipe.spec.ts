import { QuickSettlePipe } from './quick-settle.pipe';

describe('QuickSettlePipe', () => {
  it('create an instance', () => {
    const pipe = new QuickSettlePipe();
    expect(pipe).toBeTruthy();
  });
});
