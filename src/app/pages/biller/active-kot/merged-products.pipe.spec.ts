import { MergedProductsPipe } from './merged-products.pipe';

describe('MergedProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new MergedProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
